import java.util.Locale;
import java.util.Scanner;

import entities.Bank;

public class ExercicioOrientacaoBanco {

	public static void main(String[] args) {
	
		Locale.setDefault(Locale.US);
		Scanner sc = new Scanner(System.in);
		Bank bank;
		
		System.out.print("Enter account number: ");
		int number = sc.nextInt();
		sc.nextLine();
		System.out.print("Enter account holder: ");
		String holder = sc.nextLine();
		System.out.print("Is there an initial deposit (y/n)?");
		char question = sc.next().charAt(0);
		if (question == 'y') {
			System.out.print("Enter initial deposit value: ");
			double countValue = sc.nextDouble();
			bank = new Bank(number, holder, countValue);
		}
		else {
			bank = new Bank(number, holder);
		}
		
		System.out.println();
		System.out.println("Account Data:");
		bank.responseString();
		
		System.out.println();
		System.out.print("Enter a deposit value:");
		double value = sc.nextDouble();
		
		bank.accountDeposit(value);

		System.out.println("Updated account data:");
		bank.responseString();
		
		System.out.println();
		System.out.print("Enter a withdraw value: ");
		value = sc.nextDouble();
		bank.accountWithdraw(value);
		
		System.out.println("Updated account data:");
		bank.responseString();
		
		sc.close();

	}
	
	public static void exercicioSemMetodoEstatico() {
		/*Locale.setDefault(Locale.US);
		Scanner sc = new Scanner(System.in);
		
		System.out.print("Enter account number: ");
		int number = sc.nextInt();
		
		System.out.print("Enter account holder: ");
		String holder = sc.next();
		
		
		System.out.print("Is there an initial deposit (y/n)?");
		String question = sc.next();
		
		double countValue = 0;
		
		if (question.equals("y")) {
			System.out.print("Enter initial deposit value: ");
			countValue = sc.nextDouble();
			Bank bank = new Bank(number, holder, countValue);
			
			System.out.println();
			System.out.println("Account Data:");
			bank.responseString();
			
		}
		else {
			Bank bank = new Bank(number, holder);
			
			System.out.println();
			System.out.println("Account Data:");
			bank.responseString();
		}
		
		System.out.println();
		System.out.print("Enter a deposit value:");
		double value = sc.nextDouble();
		
		Bank bank = new Bank(number, holder, countValue);
		bank.accountDeposit(value);

		System.out.println("Updated account data:");
		bank.responseString();
		
		System.out.println();
		System.out.print("Enter a withdraw value: ");
		value = sc.nextDouble();
		bank.accountWithdraw(value);
		
		System.out.println("Updated account data:");
		bank.responseString();
		
		sc.close();*/

	}

}
